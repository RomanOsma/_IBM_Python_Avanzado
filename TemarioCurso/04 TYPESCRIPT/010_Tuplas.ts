(() => {


    let nombreYEdad: [string, number] = ["Alice", 30];

    // Acceder al primer elemento de la tupla (nombre)
    console.log(nombreYEdad[0]); // Muestra: "Alice"

    // Acceder al segundo elemento de la tupla (edad)
    console.log(nombreYEdad[1]); // Muestra: 30




})()