(() => {


    // Crear un array de números y asignarle algunos valores
    let numeros: number[] = [10, 20, 30, 40, 50];

    // Acceder al primer elemento del array (recuerda que los índices comienzan en 0)
    console.log(numeros[0]); // Salida: 10

    // Acceder al tercer elemento del array
    console.log(numeros[2]); // Salida: 30

})()