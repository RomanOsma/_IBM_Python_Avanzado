(() => {

    // ***************************
    //     TUPLA CON PARÁMETRO REST
    // ***************************

    let parte1: [number, string] = [1, "componente"];
    let combinado: [number, string, boolean] = [...parte1, true];

    console.log(combinado);
    console.log(parte1);







})()